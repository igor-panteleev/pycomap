# Copy to .env and fill in to run the integration test suite (tests/integration/) against
# real hardware. Loaded automatically by pytest-env. Never commit the real .env file.

PYCOMAP_TEST_HOST=192.168.1.9
PYCOMAP_TEST_ACCESS_CODE=0
PYCOMAP_TEST_PASSWORD=        # controller write-protection password (integer 0-9999)
