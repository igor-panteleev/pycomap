# Controller

The high-level entry point. Wraps [`ComApClient`](client.md) and caches the
`ConfigurationTable` on connect, enabling name-based lookup and transparent
password elevation.

::: pycomap.Controller
